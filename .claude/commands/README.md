# .claude/commands/

Comandos slash que disparam cada agente do pipeline (um por agente, 01–06).
Cada comando carrega o prompt correspondente de `agents/` e opera sobre o ciclo atual em `cycles/<id>/`.

> **Status:** a portar do material original.
