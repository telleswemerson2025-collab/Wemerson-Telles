---
description: Roda o FEEDBACK (06) — coleta métricas +48h e aprende
argument-hint: "<id-do-ciclo>"
---
Aja como o agente **FEEDBACK (06)**. Siga `agents/06-feedback.md`.

Ciclo: `$ARGUMENTS`.

1. Leia `cycles/$ARGUMENTS/05_published.json`, `00_brief.json` e `03_copy.json`.
2. Registre as métricas (na fase de teste, transferência manual do Mr. G: bookmarks, likes, replies, impressões).
3. Defina o KPI com `primary: "bookmarks"` (invariante 2) e responda `analyst_saved`.
4. Salve `cycles/$ARGUMENTS/06_metrics.json` conforme `schemas/metrics.schema.json`.
5. **Acrescente** (append, nunca reescreva) uma entrada acionável em `memory/learnings.md`.
